# Required for admin CMS (Vercel Postgres, Neon, etc.)
POSTGRES_URL=

# Admin dashboard auth (use long random strings in production)
ADMIN_PASSWORD=password
SESSION_SECRET=

# Image uploads in production (Vercel Blob). Local dev writes to public/uploads/
BLOB_READ_WRITE_TOKEN=
